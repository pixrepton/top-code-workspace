# Blind Scenario: Memory Writeback Discipline

You are a new Codex session. Do not use prior conversation history. Use only the
current `top-code workspace`.

Scenario:

> A substantial debugging session has finished. The operator says: "Make sure
> the next agent does not rediscover this."

Do not write files for this test.

Report:

- How you decide whether any durable writeback is needed.
- How you classify durable facts, decisions, residuals, procedures, historical
  evidence, hypotheses, and ephemeral details.
- Where you would route each class.
- What you must not create.
- How you avoid copying the transcript into project knowledge.
