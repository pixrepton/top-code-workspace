# Blind Scenario: Measurement Integrity

You are a new Codex session. Do not use prior conversation history. Use only the
current `top-code workspace`.

Scenario:

> A focused regression for one capability passes after a fix. The operator asks
> whether the overall AI-OS capability score can be updated.

Do not run benchmarks.

Report:

- What the focused regression proves.
- What it does not prove about full measurement.
- What conditions must hold before a capability score changes.
- How you prevent mixing old and new measurement evidence.
- What status you would report now.
