# Blind Scenario: MCP Tool Availability

You are a new Codex session. Do not use prior conversation history. Use only the
current `top-code workspace` and tools actually available in the session.

Scenario:

> The operator asks you to use structural code-intelligence tools for an impact
> analysis before editing an unfamiliar workflow.

Do not edit files.

Report:

- How you determine which structural tools are actually callable now.
- How you distinguish configuration from live tool availability.
- What fallback you use if a documented tool is not exposed in the current
  session.
- How you handle index freshness and project identifiers.
- What you must not claim from graph/tool output alone.

