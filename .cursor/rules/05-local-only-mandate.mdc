---
description: Local Docker only — no VPS/SSH unless operator explicitly requests
alwaysApply: true
---

# Local-only mandate

- **Active runtime (target):** Docker Desktop — gmail-agent `:8766` host (container `8765`), RAG `:8000`, Postgres `:54129` / GraphStore `:54130`.
- **Live state:** verify with `docker ps -a` and `scripts/preflight-local-stack.ps1` — do not assume services are up from this list alone.
- **Production:** firma w zawieszeniu — brak VPS, brak deploy na żywą stronę w backlogu (OPERATOR_DECISIONS 2026-06-17). Docker lokalny **imituje** prod; to jest jedyny aktywny cel.
- Do **not** propose SSH, VPS deploy, prod WP deploy, or curl to legacy hosts (`178.104.171.104`, `46.224.235.86`) as the next step.
- Do **not** treat `prod_deploy_missing` or similar as open P0 — see `world-state.deferred_production`.
- Historical VPS proof in docs = label `historical`, not current green.
- Gate B local smoke is the default completion bar.

Supersedes per-repo VPS deploy mandates unless operator overrides in chat.
